# ETF Finder 實作目標

更新日期：2026-09-12

本文件是後續實作與驗收的唯一進度基準。每完成一項工作，都必須重新執行對應驗收；若未通過，繼續修正，不把「程式已寫」視為完成。

## 完成定義

專案同時符合下列條件，才算可進入正式上線：

1. 版本庫可重現：原始碼、必要資料與設定範例納入版控，執行期資料、快取、密鑰與備份不污染工作樹。
2. 核心品質穩定：所有自動測試通過，CLI、排程與 API smoke test 通過。
3. 資料可信：正式 ETF 全部由可追溯的官方來源提供，持股完整度符合門檻，且每檔累積足夠真實歷史快照。
4. 營運可控：排程能區分抓取失敗與資料品質不足，診斷結果可讀、可留存、可告警。
5. 部署安全：正式環境有 TLS、服務管理、權限控管、流量限制、日誌輪替、備份與復原程序。
6. 產品可用：桌面與行動裝置的主要流程通過端到端驗收。

## M1 — 穩定開發基線（完成）

- [x] 建立 `.gitignore`，排除 Python 快取、測試快取、本機環境檔、執行日誌及腳本備份。
- [x] 從 Git 索引移除已誤追蹤的編譯快取，但保留使用者工作檔。
- [x] 預設 ETF 清單必須依目前 universe 安全解析；universe 更新後不能因舊代碼造成 CLI 失敗。
- [x] README 顯示的指定 ETF 清單必須與目前 universe 一致，並清楚區分「目前存在」與「已下架／尚未匯入」。
- [x] 為預設清單解析與空 universe 補上自動測試。
- [x] `python -m unittest discover -s tests -v` 全數通過。
- [x] `python server.py --check-history-quality ...` 必須執行到品質判定，不得因內建舊代碼回傳 `invalid_codes`。

## M2 — 每日排程可靠性（完成）

- [x] 每日腳本針對明確指定代碼執行；未指定時使用當前可用且已上市的 universe，不使用過期硬編碼清單。
- [x] 排程結果可區分：執行錯誤、抓取部分成功、歷史品質尚未達標。
- [x] 排程健康檢查顯示最近刷新資料日期、品質狀態、失敗 ETF 與下一步建議。
- [x] 排程腳本與健康檢查具備可自動執行的測試或 dry-run 驗證。

## M3 — 官方資料覆蓋

- [ ] 建立並驗證 `data/official-source-registry.json`，每個正式 ETF 代碼都有可追溯來源。
- [x] 官方來源 registry 支援來源狀態、發行商、格式、URL、每代碼 URL、啟用狀態與驗證時間。
- [x] 匯入／刷新前先驗證 registry schema、重複代碼及缺漏代碼。
- [x] 正式模式拒絕公開頁 fallback，並要求完整持股門檻。
- [x] ETF universe、profiles 與官方來源覆蓋報告一致。
- [x] 國泰 32 檔 ETF 已接上官方 PCF API，股票／債券／期貨籃子可追溯；因官方回應未提供成分權重，維持 partial 並拒絕通過正式完整度閘門。
- [x] 中國信託 22 檔 ETF 已對應官方每日基金資產頁；18 檔通過逐列完整性驗證並可視為 full，4 檔維持 partial 等待特殊代碼／間歇回應補驗。
- [x] 復華 12 檔 ETF 已完成官方基金頁與內部 ETF ID 對應；網站維護期間先維持 partial，待恢復後執行逐列 live probe 再決定是否升級 full。
- [x] 台新 11 檔 ETF 官方產品頁已全數 live probe 成功；因部分頁面同時含多個揭露區塊，先維持 partial，待 section-aware parser 排除重複後再升級 full。
- [x] 野村 10 檔 ETF 已接上官方 PCF API；最新日期與股票／債券／ETF／期貨／選擇權逐列驗證全數通過，列為 full production-ready 來源。
- [x] 兆豐 8 檔 ETF 已接上官方 PCF 表單；以內部基金 ID 維持工作階段送出查詢，並限定解析具代號與權重欄位的表格，逐列驗證全數通過。

## M4 — 歷史資料與指標完整度

- [ ] 正式 ETF 每檔至少累積 22 個合格交易日快照。
- [x] 變動分析只使用合格真實快照，部分資料不可偽裝為完整歷史。
- [ ] 12 項必要 ETF 指標完整率達 100%，或明確定義並顯示「不適用」。
- [ ] 上線資料閘門 `--check-production-data --production-min-snapshots 22` 通過。

## M5 — 正式部署與產品驗收

- [ ] 完成反向代理與 TLS、受管理服務、自動重啟及外部 uptime check。
- [ ] 啟用正式 API 驗證與使用者生命週期管理。
- [ ] 告警接到實際通知管道，並完成失敗演練。
- [ ] 備份、還原及回滾演練通過。
- [ ] 桌面與主要手機尺寸的搜尋、比較、配置、投組、Watchlist、匯出流程通過。

## 每輪實作驗收程序

1. 執行受影響功能的針對性測試。
2. 執行完整單元測試。
3. 執行設定、歷史品質與正式資料閘門；預期尚未通過的項目必須留下明確 blocker，不能是程式錯誤。
4. 檢查 `git diff`，避免納入密鑰、快取、日誌與無關檔案。
5. 更新本文件的核取狀態與「目前狀態」。

## 目前狀態

- 自動測試基線：90/90 通過。
- 正式資料閘門：未通過。
- universe 與 profiles：TWSE 官方 universe 共 240 檔，profiles 亦覆蓋 240 檔；`listedOnly=1` API 已與 universe 對齊為 240 檔。
- 官方來源進度：registry 已完成 schema 與可追溯欄位驗證；元大、群益、富邦、國泰、中國信託、復華、台新、野村與兆豐來源共覆蓋 193/240 檔，尚缺 47 檔。中國信託 18 檔、野村 10 檔與兆豐 8 檔已通過官方資料逐列驗證，production-ready 完整來源為 36/240；中國信託另 4 檔已有官方 URL 但維持 partial。兆豐 8 檔官方 PCF 表格 live probe 成功（30–52 筆）；野村 10 檔均透過官方 PCF API live probe 成功（2–54 筆，含所有資產類型）；國泰 32 檔的 29 個不重複基金代碼已全數 live probe 成功（1–280 筆），但 API 未提供成分權重；台新 11 檔均 live probe 成功（28–232 筆），因多揭露區塊可能重複暫列 partial；復華 12 檔因官網排定維護暫列 partial；元大、群益及富邦官網 HTML 也仍是 partial。
- 指標進度：官方 ETFortune 480/480 個序列請求成功；240 檔皆有部分指標，9 檔 12 項全數完整、231 檔仍缺費用率／殖利率／追蹤誤差或足夠長的報酬歷史。
- 歷史進度：22 個真實交易日快照門檻目前 0/240 通過；排程需持續累積，並另行匯入可追溯的官方歷史檔才能加速補齊。
- 已知資料 blocker：預設 adapter 仍為公開頁；官方來源尚缺 47 檔，另有 157 檔雖可追溯但尚未同時證明完整成分與可用權重；240 檔均未達 22 份合格真實快照；231 檔必要指標未完整。
- 排程狀態：2026-09-12 手動驗證完成，最近一次工作結果為 0；營運正常但資料狀態為 `data_not_ready`。
- 一致性修正：預設代碼會先與當前 universe 交集；無參數 CLI 會檢查目前全部已上市代碼。
